import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '', // Empty string or './' makes built assets use relative paths, perfect for GitHub Pages
})
